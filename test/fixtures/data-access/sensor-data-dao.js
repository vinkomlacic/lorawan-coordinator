'use strict';
// TODO: finish with mock db
module.exports = Object.freeze({
  save: async (object) => {
    return object;
  },
  saveOrUpdate: async (object) => {
    return object;
  },
  update: async (object) => {
    return object;
  },
});
